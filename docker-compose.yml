version: '3.8'

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: da_project_backend
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
